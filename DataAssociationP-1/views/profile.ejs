<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Project1</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-600 rounded-md px-3 py-2 text-sm inline-block mb-3" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-3">
            <span class="font-light">Hello ,</span> <%= user.name %>
        </h3>
        <h5>You can create a new Post!</h5>

        <form action="/createpost" method="POST" class="mt-5">
            <textarea
                placeholder="What's on your Mind?"
                class="block p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-700"
                name="content"
            ></textarea>
            <input
                class="px-3 py-2 bg-blue-600 block rounded-md mt-2 w-40"
                type="submit"
                value="Create New Post"
            />
        </form>

        <div class="posts" mt-20>
            <h3 class="text-zinc-400 mt-10">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.forEach(function(post) { %>
                    <div class="post w-1/3 p-4 mb-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        
                        <small class="mt-2 inline-block">
                            <%= post.likes ? post.likes.length : 0 %> likes
                        </small>
                        
                       <div class="btns flex gap-4 mt-1">
            <a class="text-blue-500" href="/like/<%= post._id %>">
                <% 
                const userLiked = post.likes && post.likes.some(likeId => {
                    return likeId && likeId.toString() === currentUserId;
                });
                %>
                <%= userLiked ? "Unlike" : "Like" %>
            </a>
            <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
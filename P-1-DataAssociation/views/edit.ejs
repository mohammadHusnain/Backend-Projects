<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Project1</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 border-zinc-700">
        <div class="w-full flex justify-end">
            <a class="bg-red-600 rounded-md px-3 py-2 text-sm inline-block mb-3" href="/logout">Logout</a>
        </div>
      
        <h5>Edit Your Post!</h5>

        <form action="/update/<%= post._id %>" method="POST" class="mt-5">
            <textarea
                placeholder="What's on your Mind?"
                class="block p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-700"
                name="content"
                
            ><%= post.content %></textarea>
            <input
                class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-40"
                type="submit"
                value="Update Post"
            />
        </form>
       
    </div>
</body>
</html>